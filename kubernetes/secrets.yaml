apiVersion: v1
kind: Secret
metadata:
  name: pdf-splitter-secrets
  namespace: pdf-splitter
type: Opaque
stringData:
  database-url: "postgresql://pdf_user:password@postgres:5432/pdf_splitter"
  jwt-secret-key: "your-super-secret-jwt-key"
  redis-url: "redis://:password@redis:6379/0"
